---
phase: quick
plan: 260527-n14
type: execute
wave: 1
depends_on: []
files_modified: [courtview.py]
autonomous: true
requirements: [n14]
must_haves:
  truths:
    - "All 18 cffi_requests call sites pass impersonate='chrome110'"
    - "Deployed file on RPi contains exactly 18 occurrences of impersonate"
    - "JA3 fingerprint emitted by curl_cffi with chrome110 differs from plain curl's fingerprint"
  artifacts:
    - path: "courtview.py"
      provides: "Updated Padelmates proxy calls with TLS impersonation"
      contains: "impersonate=\"chrome110\""
  key_links:
    - from: "courtview.py cffi_requests calls"
      to: "fastapi-production-fargate.padelmates.io"
      via: "curl_cffi with chrome110 BoringSSL fingerprint"
      pattern: "impersonate=\"chrome110\""
---

<objective>
Add `impersonate="chrome110"` to all 18 cffi_requests call sites in courtview.py, deploy to RPi, and prove the JA3 fingerprint changed.

Purpose: curl_cffi is imported specifically for TLS fingerprint impersonation, but currently sends plain curl's JA3 hash because no impersonate parameter is passed. chrome110 uses BoringSSL — the same TLS library as Android — making it the closest available match to OkHttp 4.9.0.

Output: Updated courtview.py committed locally, deployed to /root/projects/courtview/courtview.py on RPi, JA3 fingerprint change confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add impersonate param to all 18 cffi_requests call sites</name>
  <files>courtview.py</files>
  <action>
Read courtview.py to confirm current call patterns before editing.

There are 18 call sites grouped into four patterns. Apply targeted sed-style replacements using the Edit tool (one edit per distinct pattern — do NOT use Bash heredoc):

Pattern A (17x GET with timeout=15):
  old: `cffi_requests.get(url, headers=APP_HEADERS, timeout=15)`
  new: `cffi_requests.get(url, headers=APP_HEADERS, timeout=15, impersonate="chrome110")`

Pattern B (GET with timeout=12, if present):
  old: `cffi_requests.get(url, headers=APP_HEADERS, timeout=12)`
  new: `cffi_requests.get(url, headers=APP_HEADERS, timeout=12, impersonate="chrome110")`

Pattern C (GET with timeout=10, if present):
  old: `cffi_requests.get(url, headers=APP_HEADERS, timeout=10)`
  new: `cffi_requests.get(url, headers=APP_HEADERS, timeout=10, impersonate="chrome110")`

Pattern D (POST at line ~1951):
  old: `cffi_requests.post(url, json=..., headers=APP_HEADERS, timeout=15)`
  new: `cffi_requests.post(url, json=..., headers=APP_HEADERS, timeout=15, impersonate="chrome110")`

After applying all edits, verify the local count equals 18:
  `grep -c 'impersonate="chrome110"' courtview.py`

If the count is not 18, read the remaining unmodified lines (grep for cffi_requests without impersonate) and apply targeted edits until all 18 are covered.

Commit with message: `feat(n14): add impersonate=chrome110 to all cffi_requests calls`
  </action>
  <verify>
    <automated>grep -c 'impersonate="chrome110"' /Users/herwy/dev/courtview/courtview.py</automated>
  </verify>
  <done>Local grep returns exactly 18. Zero cffi_requests.get/post calls remain without impersonate.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy and validate JA3 fingerprint change</name>
  <files></files>
  <action>
Step 1 - Deploy:
  Run `cv-deploy` from the Mac.

Step 2 - Verify file landed:
  `rpi 'grep -c "impersonate" /root/projects/courtview/courtview.py'`
  Must return 18.

Step 3 - Verify process running:
  `rpi 'pgrep -fa "gunicorn.*courtview\|courtview.py"'`
  Must show gunicorn process.

Step 4 - Validate JA3 fingerprint change on RPi:
  Run a Python script via SSH that makes two requests to https://tls.browserleaks.com/json:
    - Request A: plain requests library (or curl_cffi with no impersonate) to get the baseline JA3
    - Request B: curl_cffi with impersonate="chrome110" to get the impersonated JA3
  Print both JA3 hashes. They must differ. If curl_cffi is not importable in the RPi's default Python path, use the venv at /root/projects/courtview/ or wherever gunicorn runs from.

  Script structure (pass inline via rpi):
    import json, urllib.request
    from curl_cffi import requests as cffi_requests
    import requests as std_requests
    baseline = json.loads(std_requests.get("https://tls.browserleaks.com/json", timeout=10).text)
    impersonated = json.loads(cffi_requests.get("https://tls.browserleaks.com/json", timeout=10, impersonate="chrome110").text)
    print("baseline JA3:", baseline.get("ja3_hash") or baseline.get("ja3"))
    print("chrome110 JA3:", impersonated.get("ja3_hash") or impersonated.get("ja3"))
    assert baseline.get("ja3_hash") != impersonated.get("ja3_hash"), "JA3 hashes are identical - impersonation not working"

  If std_requests is not installed in that env, use urllib.request for the baseline call.
  </action>
  <verify>
    <automated>rpi 'grep -c "impersonate" /root/projects/courtview/courtview.py'</automated>
  </verify>
  <done>
- Remote grep returns 18
- gunicorn process confirmed running
- Two distinct JA3 hashes printed (baseline != chrome110)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPi -> Padelmates API | Outbound TLS from RPi; fingerprint now spoofed as chrome110 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n14-01 | Spoofing | cffi_requests TLS fingerprint | accept | Intentional impersonation of a known-good client fingerprint to avoid bot detection; matches Android OkHttp's BoringSSL stack |
| T-n14-02 | Tampering | tls.browserleaks.com validation | accept | External service used only for one-time validation, not runtime path |
</threat_model>

<verification>
- `grep -c 'impersonate="chrome110"' courtview.py` returns 18 locally
- `rpi 'grep -c "impersonate" /root/projects/courtview/courtview.py'` returns 18 on RPi
- gunicorn process running on RPi
- JA3 hash with chrome110 differs from baseline hash
</verification>

<success_criteria>
All 18 cffi_requests call sites pass impersonate="chrome110". Deployed. JA3 fingerprint confirmed changed vs baseline.
</success_criteria>

<output>
Create `.planning/quick/260527-n14-add-tls-impersonation-cffi-requests/260527-n14-SUMMARY.md` when done.
</output>
