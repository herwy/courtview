---
phase: quick
plan: 260527-n14
subsystem: courtview-proxy
tags: [tls, curl-cffi, impersonation, padelmates]
dependency_graph:
  requires: []
  provides: [chrome110-tls-fingerprint]
  affects: [courtview.py]
tech_stack:
  added: []
  patterns: [curl-cffi impersonate parameter]
key_files:
  created: []
  modified:
    - courtview.py
decisions:
  - "Used chrome110 as impersonate target: BoringSSL-based, closest match to Android OkHttp 4.9.0"
metrics:
  duration: "8 minutes"
  completed: "2026-05-27T20:23:00Z"
  tasks_completed: 2
  tasks_total: 2
---

# Phase quick Plan 260527-n14: Add TLS Impersonation (curl_cffi chrome110) Summary

**One-liner:** Added `impersonate="chrome110"` to all 18 cffi_requests call sites so curl_cffi emits a BoringSSL JA3 fingerprint matching Android OkHttp instead of plain curl's fingerprint.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add impersonate param to all 18 cffi_requests call sites | bd15fd6 | courtview.py |
| 2 | Deploy and validate JA3 fingerprint change | (deploy-only) | deployed to /root/projects/courtview/courtview.py |

## Verification Results

- Local `grep -c 'impersonate="chrome110"' courtview.py` = **18**
- Remote `grep -c "impersonate" /root/projects/courtview/courtview.py` = **18**
- Gunicorn process confirmed running (PID 3748041)
- JA3 fingerprint change confirmed:
  - baseline (urllib) JA3: `88f1fd7a4ad8bf2abd56176f5455424b`
  - chrome110 JA3: `61fa0e56f05078dac9579b3f88e364fb`
  - changed: **True**

## Call Site Breakdown

18 sites across 4 patterns:

| Pattern | Count | Example |
|---------|-------|---------|
| GET timeout=15, single-line (url var) | 10 | `cffi_requests.get(url, headers=APP_HEADERS, timeout=15, impersonate="chrome110")` |
| GET timeout=15, single-line (TARGET+path) | 1 | `cffi_requests.get(TARGET + path, headers=APP_HEADERS, timeout=15, impersonate="chrome110")` |
| GET timeout=15, multi-line | 4 | membership, membership/member, proxy GET, cache-backfill |
| GET timeout=12 | 2 | `return cffi_requests.get(url, headers=APP_HEADERS, timeout=12, impersonate="chrome110")` |
| GET timeout=10 | 1 | `cffi_requests.get(url, headers=APP_HEADERS, timeout=10, impersonate="chrome110")` |
| POST timeout=15 | 1 | `cffi_requests.post(url, json=..., headers=APP_HEADERS, timeout=15, impersonate="chrome110")` |

## Deviations from Plan

None - plan executed exactly as written. The prompt specified 14 timeout=15 GET sites; actual count was 15 (14 single-line + 1 using `TARGET + path` variable). The `replace_all` pattern on `url` missed the `TARGET + path` variant, which was caught by a post-edit grep and fixed with a targeted edit before commit.

## Known Stubs

None.

## Threat Flags

None beyond what was already in the plan's threat model. T-n14-01 (intentional spoofing) is the only surface change.

## Self-Check: PASSED

- courtview.py exists and contains 18 occurrences of `impersonate="chrome110"` (verified locally and on RPi)
- Commit bd15fd6 exists: `git log --oneline | grep bd15fd6` confirmed
- JA3 hashes differ: baseline `88f1fd7a4ad8bf2abd56176f5455424b` vs chrome110 `61fa0e56f05078dac9579b3f88e364fb`
