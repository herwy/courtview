---
phase: quick
plan: 260612-we4
type: execute
wave: 1
depends_on: [260612-vxi]
files_modified:
  - courtview.py
  - courtview.html
autonomous: true
requirements: [QT-14-followup]
must_haves:
  truths:
    - "DB has compound indexes on all three archive tables for fast per-club lookups"
    - "GET /api/archive/snapshots returns timestamped list for any archive table"
    - "GET /api/archive/heatmap returns same shape as /api/heatmap for a given snapshot"
    - "GET /api/archive/club-info returns stored payload for a given snapshot"
    - "GET /api/archive/financial returns stored payload for a given snapshot and endpoint"
    - "Racketeer Club Info / Stats / Revenue tabs each have an Archive selector"
    - "Selecting a past snapshot renders archived data; selecting Live reverts to normal load"
  artifacts:
    - path: courtview.py
      provides: "3 DB indexes + 4 new Flask routes"
      contains: "archive/snapshots"
    - path: courtview.html
      provides: "Archive selector controls for Club Info, Stats, Revenue tabs"
      contains: "archive/snapshots"
  key_links:
    - from: courtview.html archive selector
      to: /api/archive/snapshots
      via: fetch on tab focus (Racketeer only)
    - from: /api/archive/heatmap
      to: archive_heatmap table
      via: "SELECT ... WHERE club_id=? AND captured_at=?"
    - from: /api/archive/club-info
      to: archive_club_info table
      via: "SELECT payload WHERE club_id=? AND captured_at=?"
    - from: /api/archive/financial
      to: archive_financial table
      via: "SELECT payload WHERE club_id=? AND captured_at=? AND endpoint=?"
---

<objective>
Wire the previously-created archive tables into a usable dashboard feature: add DB indexes for query performance, expose four read-only Flask endpoints, and add per-tab Archive selectors in the UI (Racketeer only).

Purpose: Archive tables exist but are write-only. This plan makes the stored snapshots accessible for offline research viewing without touching the live data path.
Output: courtview.py with indexes + 4 endpoints; courtview.html with Archive selector on Club Info, Stats, Revenue tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/herwy/dev/courtview/.planning/STATE.md
@/Users/herwy/dev/courtview/.planning/quick/260612-vxi-archive-racketeer-data-permanently-for-o/260612-vxi-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB indexes + archive read endpoints (courtview.py)</name>
  <files>courtview.py</files>
  <action>
Two changes to courtview.py.

**1. DB indexes in init_db()** - after the three `CREATE TABLE IF NOT EXISTS archive_*` blocks (after line 534, before `conn.commit()`), add:

```
conn.execute("CREATE INDEX IF NOT EXISTS idx_archive_heatmap_club   ON archive_heatmap   (club_id, captured_at)")
conn.execute("CREATE INDEX IF NOT EXISTS idx_archive_club_info_club  ON archive_club_info  (club_id, captured_at)")
conn.execute("CREATE INDEX IF NOT EXISTS idx_archive_financial_club  ON archive_financial  (club_id, captured_at)")
```

**2. Four new Flask routes** - add after the existing `/api/payment-history` route (after line ~1864). All four routes call `_gate()` first and return 403 via `_forbidden()` if not passed.

Route A: `GET /api/archive/snapshots`
- Params: `club_id` (required), `table` (required, whitelist: "heatmap", "club_info", "financial")
- If table not in whitelist return `{"error": "invalid table"}`, 400
- Query: `SELECT DISTINCT captured_at FROM archive_{table} WHERE club_id=? ORDER BY captured_at DESC LIMIT 50`
- Return JSON array of objects: `{"captured_at": <int>, "label": <human-readable string>}`
- Label format: `datetime.utcfromtimestamp(captured_at).strftime("%Y-%m-%d %H:%M UTC")` (import datetime is already present via `from datetime import datetime, timedelta` or add it)

Route B: `GET /api/archive/heatmap`
- Params: `club_id` (required), `captured_at` (required, cast to int)
- Query: `SELECT dow, hour, avg_occ, samples, hour_norm, dow_norm FROM archive_heatmap WHERE club_id=? AND captured_at=?`
- Build response in same shape as `/api/heatmap`: `{"club_id", "fetched_at": captured_at, "buckets": {dow: {hour: avg_occ}}, "hour_signal": {hour: hour_norm}, "dow_signal": {dow: dow_norm}}`
- Return 404 with `{"error": "no snapshot"}` if no rows found

Route C: `GET /api/archive/club-info`
- Params: `club_id` (required), `captured_at` (required, cast to int)
- Query: `SELECT payload FROM archive_club_info WHERE club_id=? AND captured_at=? LIMIT 1`
- Return the stored payload string directly as `application/json` (it was written as a JSON string; use `Response(row[0], status=200, content_type="application/json")`)
- Return 404 with `{"error": "no snapshot"}` if no row

Route D: `GET /api/archive/financial`
- Params: `club_id` (required), `captured_at` (required, cast to int), `endpoint` (required, whitelist: "revenue-summary", "payment-history")
- Query: `SELECT payload FROM archive_financial WHERE club_id=? AND captured_at=? AND endpoint=? LIMIT 1`
- Return payload as `application/json` (same pattern as Route C)
- Return 404 with `{"error": "no snapshot"}` if no row

All four routes wrap the DB call in try/except sqlite3.Error and return 500 on failure. Use `_db_connect()` + explicit `conn.close()` in a try/finally block (match existing patterns in the file).

Do NOT put fenced code blocks in the action. The description above is prose directives. Look at `api_heatmap()` (line 1120) and `api_revenue_summary()` (line 1735) for the exact coding patterns to replicate.
  </action>
  <verify>
    <automated>grep -c "archive/snapshots\|archive/heatmap\|archive/club-info\|archive/financial\|idx_archive_heatmap_club" /Users/herwy/dev/courtview/courtview.py</automated>
  </verify>
  <done>grep count is at least 8 (4 route strings + 3 index names + some overlap from index definitions). All four routes defined, three CREATE INDEX statements present inside init_db.</done>
</task>

<task type="auto">
  <name>Task 2: Archive selector UI for Racketeer (courtview.html)</name>
  <files>courtview.html</files>
  <action>
Add an Archive selector control above each of the three tabs (Club Info, Stats, Revenue) that is shown only when the selected club is Racketeer (`selectedClubId === "5111764d9bb14be3adbdb8e133e8bd80"`).

**JS constant** - near the top of the main script block add:
`const RACKETEER_CLUB_ID = "5111764d9bb14be3adbdb8e133e8bd80";`

**Helper: populateArchiveSelect(selectEl, table)** - async function that:
1. Fetches `/api/archive/snapshots?club_id=${RACKETEER_CLUB_ID}&table=${table}` (with auth token as-is for all other fetch calls in the file - check how `loadStats()` constructs its URL and replicate the same token/cookie approach)
2. Clears the select element
3. Adds a "Live" option first: `<option value="">Live</option>`
4. Appends one `<option value="${snap.captured_at}">${snap.label}</option>` per snapshot
5. On error: leave just the "Live" option

**Helper: buildArchiveSelector(tabName, table, onSnapshotLoad, onLive)** - returns a `<div class="archive-selector">` element containing:
- A `<label>` with text "Archive: "
- A `<select id="archive-select-${tabName}">` populated via populateArchiveSelect
- onChange: if value is "" call onLive(); else call onSnapshotLoad(value)

The div is hidden (`display:none`) by default and shown only for Racketeer.

**Integration in each tab:**

*Club Info tab* - In `loadClubInfo()`, if `selectedClubId === RACKETEER_CLUB_ID`:
- Ensure the archive selector div exists in the DOM above the club info content area (insert once via `insertAdjacentElement` before the content container if not already inserted)
- Show it; populate it fresh each call
- onSnapshotLoad: `fetch("/api/archive/club-info?club_id=...&captured_at="+ts)` then pass the parsed JSON to the existing render logic (extract the render steps from `loadClubInfo` into a helper `renderClubInfo(data)` if not already separated, then call `renderClubInfo(data)`)
- onLive: call the original `loadClubInfo()` re-fetch logic (or just reload the tab normally)

*Stats tab* - In `loadStats()`, same pattern but table="heatmap":
- onSnapshotLoad: fetch `/api/archive/heatmap?club_id=...&captured_at=...` then pass data to the existing heatmap render logic
- Wrap the heatmap render steps into `renderHeatmap(data)` if not already a function

*Revenue tab* - In `loadRevenueSummary()`, same pattern but table="financial" and endpoint="revenue-summary":
- onSnapshotLoad: fetch `/api/archive/financial?club_id=...&captured_at=...&endpoint=revenue-summary` then pass to render
- Wrap render steps into `renderRevenueSummary(data)` if needed

If the tab switching code at line ~2577 calls `loadStats()` directly, no change needed there - the archive selector logic lives inside `loadStats()` itself.

**CSS** - Add a small `.archive-selector` rule in the `<style>` block: `margin-bottom: 8px; font-size: 0.85rem;` and style the select to match the dashboard's existing input/select styling. Keep it minimal and unobtrusive.

Do NOT refactor existing functions beyond what is needed to extract a render helper. Do NOT change any code paths for non-Racketeer clubs. The guard `if (selectedClubId === RACKETEER_CLUB_ID)` must wrap all archive UI injection and all archive fetch calls.

Read `loadStats()`, `loadClubInfo()`, and `loadRevenueSummary()` carefully before editing to find the exact render steps to extract.
  </action>
  <verify>
    <automated>grep -c "archive/snapshots\|RACKETEER_CLUB_ID\|archive-selector\|archive/heatmap\|archive/club-info\|archive/financial" /Users/herwy/dev/courtview/courtview.html</automated>
  </verify>
  <done>grep count is at least 6. Archive selector present for all three tabs with Racketeer guard.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and verify</name>
  <files></files>
  <action>
1. Run `cv-deploy` from /Users/herwy/dev/courtview.
2. Restart gunicorn on the RPi using the Python subprocess.Popen pattern (per rpi-ssh.md - never use nohup + &):

```
rpi 'pkill -f "gunicorn.*courtview" || true'
```

Then start fresh:
```
rpi 'python3 -c "import subprocess; subprocess.Popen([\"gunicorn\",\"--daemon\",\"--bind\",\"0.0.0.0:8766\",\"--certfile=/root/labs/certs/labs.doxx.crt\",\"--keyfile=/root/labs/certs/labs.doxx.key\",\"--error-logfile=/root/projects/courtview/courtview.log\",\"--workers=2\",\"courtview:app\"],cwd=\"/root/projects/courtview\",stdin=open(\"/dev/null\"),stdout=open(\"/dev/null\",\"w\"),stderr=subprocess.STDOUT,close_fds=True,start_new_session=True)"'
```

Wait ~3 seconds, then verify:

3. Verify courtview.py landed: `rpi 'grep -c "archive/snapshots\|archive/heatmap\|archive/club-info\|archive/financial" /root/projects/courtview/courtview.py'`
4. Verify courtview.html landed: `rpi 'grep -c "archive/snapshots\|RACKETEER_CLUB_ID" /root/projects/courtview/courtview.html'`
5. Verify gunicorn running: `rpi 'pgrep -fa "gunicorn.*courtview"'`
6. Smoke-test the snapshots endpoint (will return empty array if no data yet, that is fine): `rpi 'python3 -c "import http.client, ssl; ctx=ssl.create_default_context(); ctx.load_verify_locations(\"/root/labs/certs/doxx-root-ca.crt\"); conn=http.client.HTTPSConnection(\"192.168.8.175\",8766,context=ctx,timeout=5); t=open(\"/root/.courtview_token\").read().strip(); conn.request(\"GET\",\"/api/archive/snapshots?club_id=5111764d9bb14be3adbdb8e133e8bd80&table=heatmap\",headers={\"Cookie\":\"courtview_token=\"+t}); r=conn.getresponse(); print(r.status, r.read(200))"'`

All four counts must be > 0 and gunicorn must show workers running before reporting done.
  </action>
  <verify>
    <automated>rpi 'grep -c "archive/snapshots" /root/projects/courtview/courtview.py'</automated>
  </verify>
  <done>Remote grep count > 0, pgrep shows gunicorn workers, snapshots endpoint returns HTTP 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Flask /api/archive/* | Authenticated by courtview_token cookie; same boundary as all existing /api/* routes |
| Flask -> SQLite | Internal; no external input reaches SQL without parameterised queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-we4-01 | Tampering | table param in /api/archive/snapshots | mitigate | whitelist check: only "heatmap", "club_info", "financial" accepted; anything else returns 400 |
| T-we4-02 | Information Disclosure | archive endpoints expose stored financial/PII-adjacent data | accept | endpoints are gated by courtview_token (same auth as all other routes); archive data is already stored locally |
| T-we4-03 | Injection | captured_at param passed to SQL | mitigate | cast to int before use; SQL uses parameterised query - invalid value raises ValueError before DB call |
| T-we4-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
- `grep -c "idx_archive_heatmap_club\|idx_archive_club_info_club\|idx_archive_financial_club" courtview.py` returns 3
- `grep -c "archive/snapshots\|archive/heatmap\|archive/club-info\|archive/financial" courtview.py` returns at least 8
- `grep -c "archive/snapshots\|RACKETEER_CLUB_ID\|archive-selector" courtview.html` returns at least 3
- Remote: grep counts > 0 on deployed files
- Remote: snapshots endpoint returns HTTP 200 with JSON array
</verification>

<success_criteria>
- Three CREATE INDEX statements inside init_db() for archive tables
- Four new Flask routes: /api/archive/snapshots, /api/archive/heatmap, /api/archive/club-info, /api/archive/financial
- Archive selector visible on Club Info, Stats, Revenue tabs when Racketeer is selected
- Selecting a past snapshot renders archived data using existing render functions
- Selecting "Live" reverts to normal live-fetch behaviour
- All changes deployed and verified on RPi with grep count > 0
</success_criteria>

<output>
Create `.planning/quick/260612-we4-wire-up-archive-data-to-dashboard-ui-and/260612-we4-SUMMARY.md` when done.
</output>
