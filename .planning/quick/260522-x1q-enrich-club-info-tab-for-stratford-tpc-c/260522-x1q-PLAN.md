---
phase: quick
plan: 260522-x1q
type: execute
wave: 1
depends_on: []
files_modified:
  - courtview.py
  - courtview.html
autonomous: true
requirements: [x1q-enrich-tpc-club-info]
must_haves:
  truths:
    - "Club Info tab for Stratford TPC shows real name, address, email, courts, and hours"
    - "Club Info tab shows an Activities & Programs section grouped by activity group"
    - "Data comes from live TPC API calls, not a hardcoded stub"
    - "If either TPC API call fails, the tab still renders with whatever data is available"
  artifacts:
    - path: "courtview.py"
      provides: "_api_club_info_tpc making live ObtenerInformacionCentro + ObtenerConfiguracionSistemaReservaPlazas calls"
      contains: "ObtenerInformacionCentro"
    - path: "courtview.html"
      provides: "Activities & Programs section in renderClubInfo"
      contains: "Activities & Programs"
  key_links:
    - from: "courtview.py _api_club_info_tpc"
      to: "_tpc_post"
      via: "two POST calls"
      pattern: "_tpc_post.*ObtenerInformacionCentro"
    - from: "courtview.html loadClubInfo"
      to: "renderClubInfo"
      via: "data.activities passed as 5th argument"
      pattern: "renderClubInfo.*activities"
---

<objective>
Replace the hardcoded static stub in `_api_club_info_tpc` with live calls to the TPC Matchpoint API, then surface the returned club data and activity catalog in the Club Info tab.

Purpose: The Club Info tab currently shows placeholder data for Stratford TPC. This enriches it with real opening hours, court count, address, and a full grouped activity catalog.
Output: Live club info + activities visible in the Club Info tab for Stratford TPC.
</objective>

<execution_context>
@/Users/herwy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herwy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/herwy/dev/courtview/CLAUDE.md
@/Users/herwy/dev/courtview/courtview.py
@/Users/herwy/dev/courtview/courtview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _api_club_info_tpc with live TPC API calls</name>
  <files>courtview.py</files>
  <action>
Replace the body of `_api_club_info_tpc` (lines 863-890) so it:

1. Calls `_tpc_post("/services/mobi/appservices/v1/club.svc/ObtenerInformacionCentro", {"idCentro": 2})` to fetch club profile.
2. Calls `_tpc_post("/services/mobi/appservices/v1/club.svc/ObtenerConfiguracionSistemaReservaPlazas", {})` to fetch the activity catalog.
3. Builds the response payload from real API data, falling back to the current static values if a call raises an exception or returns no `Respuesta` key.

The fallback static stub (current body) should remain accessible as a local dict literal so the except branch can use it without code duplication.

**Profile construction** from `ObtenerInformacionCentro` `Respuesta`:
- `name` - `Nombre`
- `address` - `Direccion`
- `email` - `Email`
- `courts` - `NumeroPistas`
- `twitter` - `CuentaTwitter`
- `facilities` - list of `Texto` strings from `PastillasInformacionInfoGeneral`
- `opening_hours` - array of 7 identical `{"open": "08:00", "close": "23:00"}` entries (one per day, index 0=Mon to 6=Sun), derived from the `Horario` field ("HH:MM-HH:MM"). Parse the open and close times by splitting on `-`. If `Horario` is missing or malformed, default to `{"open": "08:00", "close": "23:00"}` for all 7 days.

**Activity construction** from `ObtenerConfiguracionSistemaReservaPlazas` `Respuesta.Actividades`:
- Build a hardcoded `GROUP_NAMES` dict (keyed by integer group Id):
  - `{1: "Group Training", 2: "Private Classes", 3: "SPC Tournament", 4: "Social / Community", 6: "Competitive Training", 8: "Kids Programs", 12: "Starter / Taster Sessions", 19: "Physiotherapy & Wellness"}`
- Group activities by `Id_Grupo`. For each activity use `{"id": Id, "name": Nombre, "group_id": Id_Grupo, "group_name": GROUP_NAMES.get(Id_Grupo, f"Group {Id_Grupo}")}`.
- Sort groups by group_id; sort activities within each group alphabetically by name.
- Produce a flat list under the key `activities` in the payload, where each element is one activity dict as above.

**Final payload keys**: `profile`, `memberships` (empty list), `credits` (empty list), `extras` (empty list), `activities` (list as above).

Cache the payload in `club_info_cache` exactly as the current code does (INSERT OR REPLACE, 10-min TTL via the existing caller logic).

Wrap each `_tpc_post` call individually in try/except so a failure in the activity fetch does not prevent the profile from being returned (and vice versa). If the profile call fails, use the static fallback dict for `profile`. If the activity call fails, set `activities` to `[]`.
  </action>
  <verify>
    <automated>rpi 'python3 -c "
import sys; sys.path.insert(0, \"/root/projects/courtview\")
# Just import to check syntax
import importlib.util, os
spec = importlib.util.spec_from_file_location(\"cv\", \"/root/projects/courtview/courtview.py\")
mod = importlib.util.module_from_spec(spec)
# We only want parse check
import py_compile; py_compile.compile(\"/root/projects/courtview/courtview.py\", doraise=True)
print(\"syntax ok\")
"'</automated>
  </verify>
  <done>
    courtview.py has no syntax errors, `_api_club_info_tpc` body contains `ObtenerInformacionCentro` and `ObtenerConfiguracionSistemaReservaPlazas` calls, and the fallback static dict is present in the except branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Activities & Programs section to renderClubInfo</name>
  <files>courtview.html</files>
  <action>
Two changes to `courtview.html`:

**A. `loadClubInfo` (around line 1797):** Extract `data.activities` and pass it as a 5th argument to `renderClubInfo`:

```
const activities = Array.isArray(data.activities) ? data.activities : [];
out.innerHTML = renderClubInfo(profile, memberships, credits, extras, activities);
```

**B. `renderClubInfo` signature and body:**

Change the signature to: `function renderClubInfo(profile, members, credits, extras, activities)`

After the existing Extras section (after the closing `</div></section>` for extras, before the empty-state check at line 1954), add an Activities & Programs section. Render it only when `activities` is a non-empty array.

Section structure:
- Group activities by `group_name` in insertion order (use a Map keyed by group_name to preserve the server's group_id sort order).
- For each group: render a `<section class="sec">` with a `<h2 class="sec-title">` showing the group name and a `<div class="sec-meta">` showing the count (e.g. "8 activities").
- Inside, render a `<ul class="activity-list">` with `<li class="activity-item">` for each activity name (escaped).

Update the empty-state guard to also check `activities`:
```js
if (!plans.length && !packs.length && !hours && !extrasList.length && !activities?.length) {
```

No new CSS classes needed for the section/heading - reuse existing `.sec`, `.sec-hd`, `.sec-title`, `.sec-meta` classes. Add minimal inline styles for `.activity-list` and `.activity-item` using a `<style>` tag injected into `h` if those classes do not already exist in the stylesheet, or add them as inline styles on the elements directly: `list-style:none; padding:0; margin:0` on the list, `padding:4px 0; font-size:13px; color:var(--text-2,#ccc)` on each item.

Do not touch any other function. Do not change the `loadMemberCounts` call.
  </action>
  <verify>
    <automated>grep -c "Activities \&amp; Programs\|activities\.length\|activity-list" /Users/herwy/dev/courtview/courtview.html</automated>
  </verify>
  <done>
    `renderClubInfo` accepts a 5th `activities` parameter, the Activities & Programs section is rendered when activities are present, and the empty-state guard includes the activities check.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy and verify on RPi</name>
  <files></files>
  <action>
1. Run `cv-deploy` to push both files.
2. Verify the new TPC API call string landed in the deployed Python file.
3. Verify the activities section marker landed in the deployed HTML file.
4. Check gunicorn is running.
5. Hit the `/api/club-info` endpoint for the TPC club UUID to confirm the live response includes `activities` with at least one entry and that `profile.name` is "Stratford Padel Club" (not the stub "Stratford, London" address).

TPC club UUID: `tpc-stratford` (check ALLOWED_PATHS / `isTPCClub` logic in courtview.py for the exact club_id string used, then substitute into the curl-equivalent below).

Use the single-SSH Python http.client pattern from CLAUDE.md to hit the endpoint (dashboard curl is blocked on port 8765, but port 8766 is courtview - use Python http.client on the RPi):

```
rpi 'python3 -c "
import http.client, json
with open(\"/root/.courtview_token\") as f: T = f.read().strip()
conn = http.client.HTTPConnection(\"192.168.8.175\", 8766, timeout=10)
conn.request(\"GET\", \"/api/club-info?club_id=tpc-stratford\", headers={\"Cookie\": \"courtview_token=\" + T})
r = conn.getresponse()
d = json.loads(r.read())
print(\"status\", r.status)
print(\"name\", d.get(\"profile\",{}).get(\"name\",\"MISSING\"))
print(\"activities\", len(d.get(\"activities\",[])))
"'
```

Replace `tpc-stratford` with the actual club_id string if different (grep courtview.py for `TPC_CLUB_ID` or `isTPCClub` to confirm).
  </action>
  <verify>
    <automated>rpi 'grep -c "ObtenerInformacionCentro" /root/projects/courtview/courtview.py && grep -c "activity-list\|Activities" /root/projects/courtview/courtview.html'</automated>
  </verify>
  <done>
    Both grep counts are greater than 0, gunicorn is running, and the `/api/club-info` response for TPC shows `profile.name = "Stratford Padel Club"` and `activities` array with 10+ entries.
  </done>
</task>

</tasks>

<verification>
- `rpi 'grep -c "ObtenerInformacionCentro" /root/projects/courtview/courtview.py'` returns >= 1
- `rpi 'grep -c "activity-list" /root/projects/courtview/courtview.html'` returns >= 1
- `/api/club-info` response contains `"name": "Stratford Padel Club"` and `activities` list with entries
- gunicorn process is running on port 8766
</verification>

<success_criteria>
Club Info tab for Stratford TPC shows:
- Real club name, address (221 High Street E15 2 LONDON), email (info@stratfordpadelclub.org)
- Opening hours grid showing 08:00-23:00 for all 7 days
- Activities & Programs section with groups (Group Training, Private Classes, Kids Programs, etc.) and their activity names
- No crashes or 500 errors on the endpoint
</success_criteria>

<output>
Create `.planning/quick/260522-x1q-enrich-club-info-tab-for-stratford-tpc-c/260522-x1q-SUMMARY.md` when done.
</output>
